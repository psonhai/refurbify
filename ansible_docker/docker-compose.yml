version: "3.8"

services:
  ansible:
    build: .
    network_mode: "host"
    container_name: ansible-controller
    volumes:
      - ../ansible_server_files:/ansible
    working_dir: /ansible
    tty: true